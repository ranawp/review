import React from 'react';

const Blog = () => {
    return (
        <div className='container mt-5'>
            <h3>
                What is context API?
            </h3>
            <p>
                The React context API creates global variables that can be used directly in different components. This is an alternative to prop drilling. This is moving props from grandparent to child to parent.Context api a new feature. Its added in 16.3 version that allows one to share state to another state. This is use very simple way.
            </p>
            <h3>Semantic tag: </h3>
            <p>
                semantic HTML that introduces meaning to the web pages.
                The semantic element is clearly communicated with the browser and developer. This element clearly defines its content. A semantic tag is good for SEO optimization. search engines can understand easily by using semantic tags.
            </p>
        </div>
    );
};

export default Blog;